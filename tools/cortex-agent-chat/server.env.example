# Cortex Agent Chat - Backend Configuration (server-side, safe template)
# Copy to .env.server.local (never commit that file)

# Snowflake account identifier (e.g., xy12345.us-east-1 or orgname-accountname)
SNOWFLAKE_ACCOUNT=your_account_here

# Snowflake service user (created by deploy_with_key.sql)
SNOWFLAKE_USER=SFE_CORTEX_DEMO_USER

# Database and schema where Cortex Agent is deployed
SNOWFLAKE_DATABASE=SNOWFLAKE_EXAMPLE
SNOWFLAKE_SCHEMA=SFE_CORTEX_AGENT_CHAT

# Cortex Agent name
SNOWFLAKE_AGENT_NAME=SFE_REACT_DEMO_AGENT

# RSA Private Key (keep server-side; never commit the key)
#
# Provide ONE of the following:
#
# Option A (recommended): point to a local key file
# - Generated by tools/01_setup.sh as rsa_key.pem
SNOWFLAKE_PRIVATE_KEY_PATH=/absolute/path/to/rsa_key.pem
#
# Option B: inline key material as a single line with literal \n characters
# - Generated by tools/01_setup.sh into .env.server.local
# SNOWFLAKE_PRIVATE_KEY_PEM="(generated value with \\n sequences)"

# Backend port (default: 4000)
PORT=4000
