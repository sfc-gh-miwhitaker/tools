# Snowflake Connection Configuration - TEMPLATE FILE
#
# SECURITY: This file contains NO real credentials and is safe to commit.
#           Copy this file to .env.local and fill in your ACTUAL values.
#           NEVER commit .env.local - it will contain your real private key!

# Snowflake account identifier (e.g., xy12345.us-east-1 or orgname-accountname)
REACT_APP_SNOWFLAKE_ACCOUNT=your_account_here

# Snowflake username (setup script creates SFE_CORTEX_DEMO_USER)
REACT_APP_SNOWFLAKE_USER=SFE_CORTEX_DEMO_USER

# Database and schema where Cortex Agent is deployed
REACT_APP_SNOWFLAKE_DATABASE=SNOWFLAKE_EXAMPLE
REACT_APP_SNOWFLAKE_SCHEMA=SFE_CORTEX_AGENT_CHAT

# Cortex Agent name
REACT_APP_CORTEX_AGENT_NAME=SFE_REACT_DEMO_AGENT

# Backend: the private key now belongs in server/.env.server.local (not in the browser)
# See README for backend environment variables.
